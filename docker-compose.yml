services:
  go_web_app:
    build:  
      context: .
      dockerfile: Dockerfile.prod
    container_name: go_web_app
    ports:
      - "3000:3000"
    env_file: 
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: always
